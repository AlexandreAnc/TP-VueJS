# Laisser vide en dev : les appels /api/* passent par le proxy Vite → http://127.0.0.1:3000
# VITE_API_URL=https://api.tp-vuejs.aanc.fr

# Connexion admin + reCAPTCHA v3 (production : build avec ces variables sur l’hébergeur, sans bypass)
VITE_RECAPTCHA_DEV_BYPASS=1

# Clé publique (non secrète) : utile pour `vite build` / preview sans `.env` séparé ; Docker n’embarque pas les .env (voir .dockerignore).
VITE_RECAPTCHA_SITE_KEY=6LckP6MsAAAAABOf5NilEJwwcrCq-LiOIQqxq_Y9
